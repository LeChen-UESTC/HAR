base_config: har_experiment.yaml

experiment:
  variant_name: label_part_aware
  tags: ["skeleton-gircse", "label_part_aware"]

text_branch:
  description_variant: label_only

model:
  projector:
    type: part_aware_qformer
