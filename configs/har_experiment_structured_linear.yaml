base_config: har_experiment.yaml

runtime:
  cuda_visible_devices: "3"

experiment:
  variant_name: structured_linear
  tags: ["skeleton-gircse", "structured_linear"]

model:
  projector:
    type: linear

train_presets:
  skeleton_gircse:
    train:
      batch_size: 4
      gradient_accumulation_steps: 8
